# Task 13: Bootstrap сервера

## Цель

`bootstrap.sh`: Docker, compose, git/curl/make, ufw, user `deploy`.

## Файлы

- `devops/server/bootstrap.sh`
- `devops/server/README.md` § Bootstrap

## DoD

Скрипт выполнен на `201.51.4.34`; `docker compose version` OK.
