# Route all Grafana-managed alerts to Telegram bridge
apiVersion: 1

policies:
  - orgId: 1
    receiver: diaai-telegram
    group_by:
      - grafana_folder
      - alertname
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
